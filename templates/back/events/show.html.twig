{% extends 'baseBack.html.twig' %}

{% block title %}evenement{% endblock %}

{% block body %}
             <!-- Section Title -->
             <br><br><br>
      <div class="container section-title" data-aos="fade-up">
        <span class="">event<br></span>
        <h2 class="">event<br></h2>
        <p>full details about the selected event </p>
      </div><!-- End Section Title -->

    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evenement.id }}</td>
            </tr>
            <tr>
                <th>nom</th>
                <td>{{ evenement.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ evenement.description }}</td>
            </tr>
            <tr>
                <th>dateDebut</th>
                <td>{{ evenement.dateDebut ? evenement.date|date('Y-m-d H:i:s') : '' }}</td>
                
            </tr>

             <tr>
                <th>dateFin</th>
                <td>{{ evenement.dateFin ? evenement.date|date('Y-m-d H:i:s') : '' }}</td>
                
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_eventsBack_index') }}">back to list</a>

   <a href="{{ path('app_tachesBack_index2', {'id': evenement.id}) }}">show tasks</a>
<br><br>

    {{ include('back/events/_delete_form.html.twig') }}
    <br><br><br>
{% endblock %}
