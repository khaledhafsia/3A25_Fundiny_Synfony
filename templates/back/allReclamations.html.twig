{% extends 'baseBack.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row mb-1">
            <div class="col-md-6 text-left">
                <a href="{{ path('app_reponses_index_back') }}" class="btn btn-primary mr-2">Reponses</a>
                <a href="{{ path('app_reclamations_export_excel') }}" class="btn btn-info mr-2">
                    <i class="fas fa-file-excel"></i> Export Excel
                </a>
                <a href="{{ path('app_reclamations_export_pdf') }}" class="btn btn-danger">
                    <i classa="fas fa-file-pdf"></i> Export PDF
                </a>
            </div>
        </div>

        {{ render(path('reclamations_list')) }}
    </div>
{% endblock %}
