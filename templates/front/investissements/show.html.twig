{% extends 'base.html.twig' %}

{% block title %}Investissements{% endblock %}

{% block body %}
             <!-- Section Title -->
             <br><br><br>
      <div class="container section-title" data-aos="fade-up">
        <span class="">Invesment<br></span>
        <h2 class="">Invesment<br></h2>
        <p>full details about the selected investment </p>
      </div><!-- End Section Title -->

    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ investissement.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ investissement.montant }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ investissement.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ investissement.date ? investissement.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_investissements_index') }}">back to list</a>

    <a href="{{ path('app_investissements_edit', {'id': investissement.id}) }}">edit</a>

    <a href="{{ path('app_taches_new2', {'id': investissement.id}) }}">add task</a>

   <a href="{{ path('app_taches_index2', {'id': investissement.id}) }}">show tasks</a>
<br><br>

    {{ include('front/investissements/_delete_form.html.twig') }}
    <br><br><br>
{% endblock %}
